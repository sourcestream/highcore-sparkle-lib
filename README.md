Highcore library for sparkle formation

Include in Gemfile of your highcore template:
```ruby
gem 'highcore_sparkle_lib'
```

Include in the template requirements:
```yaml
requirements:
  - highcore_sparkle_lib
```
